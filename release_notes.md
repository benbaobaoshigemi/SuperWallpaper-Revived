# v1.0.0

首个正式发布。

## 功能
- **AOD 归还主题**：屏蔽超级壁纸接管，息屏继续显示当前主题（锁屏时钟或全屏 AOD「和锁屏样式一致」）
- **桌面 → 锁屏转场**：Filament（地球 / 月球）、Unity（火星 / 土星 / 几何）、雪山均保留转场动画
- **锁屏 → AOD 静帧**：从锁屏息屏不重发场景动画、不刷新样式
- **唤醒不跳变**：月球不再重随机位相，地球不按唤醒时刻重算太阳构图
- **全屏 AOD**：为小米 14 Ultra（aurora）启用「息屏样式 = 和锁屏样式一致」
- **自定义锁屏**：恢复长按锁屏「自定义」入口与设置页预览刷新

## 安装
在 LSPosed 中启用模块并按 README 勾选作用域；设置 → 息屏 → 息屏样式可启用全屏 AOD。
